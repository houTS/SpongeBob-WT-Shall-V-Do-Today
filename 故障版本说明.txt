在使用时会发生目前无法解决的故障
CMDcore为定时触发
在 通讯配置.VI 中，服务端模式，初步更新服务端信息时，只要在值引用元素同址 中写入任何数据，就会导致CMDCore循环的类私有数据被清空（即值引用的转数字记录，和队列引用等）
后续发现，在模式切换时触发旧纪录清楚（旧模式为服务端）导致的写入也会导致相同的表现
即，服务端相应数据写入会导致无法理解的故障
奇怪的是串口模式和客服端模式都可以操作的，他们的数据都在一起

太奇怪了，只要有写入就会出现。原理未知。更改方法未知...